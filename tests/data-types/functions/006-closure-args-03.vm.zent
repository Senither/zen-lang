--TEST--
It fails when calling closure with wrong number of arguments
--FILE--
func createSum(a) {
    return func(b) {
        return a + b;
    };
}

var sum = createSum(10);

println(sum());
--ERROR--
wrong number of arguments to `<anonymous>`: got 0, want 1
    at <unknown>:0:0
