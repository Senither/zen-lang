--TEST--
It doesn't mutate outer scope variable when using same name in filter function
--FILE--
var x = 'test';
var arr = [1,2,3,4];
var result = arrays.filter(arr, func(x) { return x > 2; });

println(result);
println(arr);
println(x);
--EXPECT--
[3, 4]
[1, 2, 3, 4]
test
