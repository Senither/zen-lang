--TEST--
It fails if given non array as argument
--FILE--
var a = [1, 2];

println(arrays.concat(a, 'not an array'));
--ERROR--
all arguments to `concat` must be arrays, got STRING
    at <unknown>:0:0
