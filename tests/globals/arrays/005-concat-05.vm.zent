--TEST--
It fails if given non array as argument
--FILE--
var a = [1, 2];

println(arrays.concat(a, 'not an array'));
--ERROR--
argument 2 to `concat` has invalid type: got STRING, want ARRAY
    at <unknown>:0:0
