--TEST--
It fails when given non-array as first argument
--FILE--
println(arrays.first(5, func(x) { return x > 2; }));
--ERROR--
argument 1 to `first` has invalid type: got INTEGER, want ARRAY
    at <unknown>:0:0
